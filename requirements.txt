# Triadic Emergent Duality — Python Dependencies
#
# Requires: Python >= 3.10
# Install: pip install -r requirements.txt
# GPU support: install torch with CUDA separately first
#   pip install torch --index-url https://download.pytorch.org/whl/cu124

# Core ML
torch>=2.0
transformers>=4.30

# Scientific
numpy>=1.24
scipy>=1.10

# Visualization
matplotlib>=3.7
tqdm>=4.60

# Timeline analysis (install from local: pip install -e ../reptimeline)
reptimeline>=0.1.0

# Optional: interactive plots
# plotly>=5.0
